"""Load generation and query benchmarking for database work.

This package is deliberately not a Django app: it holds no models and needs no
migrations. It sits on the import path because manage.py runs from backend/.
"""
